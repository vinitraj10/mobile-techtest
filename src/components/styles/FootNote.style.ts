import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    footer: {
        backgroundColor: '#ddd',
        padding: 8,
        justifyContent: 'center',
        alignItems: 'center',
    },
    whichLink: {
        color: 'blue',
        fontWeight: '800',
    },
});
