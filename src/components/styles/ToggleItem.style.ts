import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    toggleItem: {
        padding: 10,
        margin: 10,
        borderWidth: 1,
        borderColor: '#ddd',
    },
});
